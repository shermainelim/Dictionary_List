using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace DictionaryFundamentals
{
    //class Hashset
    //{
    //    static void _Main(string[] args)
    //    {
    //        HashSet<string> words= new HashSet<string>(new string[] { "All", "work", "and", "no", "play", "makes", "for", "no", "work", "no", "results"});
    //        HashSet<int> occurence = new HashSet<int>(new int[] { });

    //        foreach(string word in words)
    //        {
    //            int count = 0;
    //            foreach (string word1 in words)
    //            {
    //                if (word.Equals(word1))
    //                {
    //                    count++;
    //                }
    //            }
    //            occurence.Add(count);
    //        }

    //        Console.WriteLine(occurence.Max());
    //    }
    //}
}
